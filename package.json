{
  "name": "structurev2-prototype",
  "private": true,
  "version": "0.0.0",
  "description": "StructureV2 — D1 prototype (Cloudflare Pages + Pages Functions + D1)",
  "scripts": {
    "install:all": "npm install && npm install --prefix app",
    "dev": "npm run dev --prefix app",
    "build": "npm run build --prefix app && node scripts/bundle.mjs",
    "preview": "wrangler pages dev dist --d1=DB"
  }
}
